<Window x:Class="CRM.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CRM" 
        Height="450" Width="300"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="20">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Login" Style="{StaticResource FieldLabel}"/>
        <TextBox x:Name="Login" Grid.Row="1" Style="{StaticResource FieldTextBox}"/>

        <TextBlock Grid.Row="3" Text="Password" Style="{StaticResource FieldLabel}"/>
        <TextBox x:Name="Password" Grid.Row="4" Style="{StaticResource FieldTextBox}"/>

        <Button Grid.Row="6" Content="Login" Style="{StaticResource PrimaryButton}" Click="LoginBtn"/>
        <Button Grid.Row="7" Content="Register" Style="{StaticResource PrimaryButton}" Click="RegisterBtn"/>
        
    </Grid>
</Window>