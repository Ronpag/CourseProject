<Window x:Class="CRM.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Icon="pack://application:,,,/Assets/relic.ico"
        Title="CRM"
        MaxHeight="400" MinWidth="400"
        Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource SurfaceBrush}">

    <Grid Margin="28,28,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CRM" FontSize="28" FontWeight="Bold"
                   Foreground="{StaticResource PrimaryBrush}"
                   HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="2" Text="Login" Style="{StaticResource FieldLabel}"/>
        <TextBox x:Name="Login" Grid.Row="3" Style="{StaticResource FieldTextBox}"/>

        <TextBlock Grid.Row="5" Text="Password" Style="{StaticResource FieldLabel}"/>
        <TextBox x:Name="Password" Grid.Row="6" Style="{StaticResource FieldTextBox}"/>

        <Button Grid.Row="8" Content="Sign In" Style="{StaticResource PrimaryButton}" Click="LoginBtn" Height="36"/>
        <Button Grid.Row="9" Content="Create Account" Style="{StaticResource SecondaryButton}" Click="RegisterBtn"/>
    </Grid>
</Window>
