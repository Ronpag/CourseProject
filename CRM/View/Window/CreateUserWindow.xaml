<Window x:Class="CRM.View.CreateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CRM" 
        Height="380" Width="350"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="User Name"/>
        <TextBox x:Name="UserNameBox" Grid.Row="1" Margin="0,5,0,10"/>

        <TextBlock Grid.Row="2" Text="Login"/>
        <TextBox x:Name="LoginBox" Grid.Row="3" Margin="0,5,0,10"/>

        <TextBlock Grid.Row="4" Text="Password"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="5" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="6">
            <CheckBox x:Name="ActiveCheckBox" Content="Active" IsChecked="True"/>
        </StackPanel>

        <Button Grid.Row="7" Margin="0,15,0,0" Height="35" Content="Register" Click="RegisterBtn"/>

    </Grid>

</Window>
