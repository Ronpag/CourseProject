<Page x:Class="CRM.ViewModel.UserWindow.UserStatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="My Statistics" FontSize="20" FontWeight="Bold"/>
        <TextBlock x:Name="TotalTasks" Grid.Row="1" Margin="0,15,0,0" FontSize="14"/>
        <TextBlock x:Name="AssignedTasks" Grid.Row="2" Margin="0,5,0,0" FontSize="14"/>
        <TextBlock x:Name="InProgressTasks" Grid.Row="3" Margin="0,5,0,0" FontSize="14"/>
        <TextBlock x:Name="CompletedTasks" Grid.Row="4" Margin="0,5,0,0" FontSize="14"/>
        <TextBlock x:Name="PendingReview" Grid.Row="5" Margin="0,5,0,0" FontSize="14"/>
    </Grid>
</Page>
