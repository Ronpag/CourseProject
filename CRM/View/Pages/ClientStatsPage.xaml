<Page x:Class="CRM.ViewModel.ClientWindow.ClientStatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24,20,24,20">
        <StackPanel>
            <TextBlock Text="My Statistics" Style="{StaticResource PageTitle}"/>

            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Date Filter" Style="{StaticResource SectionTitle}"/>
                    <WrapPanel Margin="0,8,0,0">
                        <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,8,0"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                        <DatePicker x:Name="DateFrom" Width="140" Style="{StaticResource FieldDatePicker}" Margin="0"/>
                        <TextBlock Text="To:" VerticalAlignment="Center" Margin="16,0,8,0"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                        <DatePicker x:Name="DateTo" Width="140" Style="{StaticResource FieldDatePicker}" Margin="0"/>
                        <Button Content="Apply" Click="ApplyFilter" Width="90" Height="30"
                                Style="{StaticResource PrimaryButton}" Margin="12,0,0,0"/>
                        <Button Content="Reset" Click="ResetFilter" Width="90" Height="30"
                                Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <StackPanel Margin="0,8,0,0">
                <TextBlock x:Name="TotalOrders" Margin="0,0,0,6" FontSize="14"/>
                <TextBlock x:Name="PendingOrders" Margin="0,0,0,6" FontSize="14"/>
                <TextBlock x:Name="AvailableOrders" Margin="0,0,0,6" FontSize="14"/>
                <TextBlock x:Name="AssignedOrders" Margin="0,0,0,6" FontSize="14"/>
                <TextBlock x:Name="InProgressOrders" Margin="0,0,0,6" FontSize="14"/>
                <TextBlock x:Name="CompletedOrders" Margin="0,0,0,6" FontSize="14"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
