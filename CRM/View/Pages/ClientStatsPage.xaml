<Page x:Class="CRM.ViewModel.ClientWindow.ClientStatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20" Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="My Statistics" FontSize="20" FontWeight="Bold"/>

            <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,12,0,0">
                <StackPanel>
                    <TextBlock Text="Date Filter" Style="{StaticResource CardTitle}"/>
                    <WrapPanel Margin="0,6,0,0">
                        <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <DatePicker x:Name="DateFrom" Width="130"/>
                        <TextBlock Text="To:" VerticalAlignment="Center" Margin="12,0,6,0"/>
                        <DatePicker x:Name="DateTo" Width="130"/>
                        <Button Content="Apply" Click="ApplyFilter" Width="80" Margin="12,0,0,0" Height="26"/>
                        <Button Content="Reset" Click="ResetFilter" Width="80" Margin="6,0,0,0" Height="26"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="2" Margin="0,15,0,0">
                <TextBlock x:Name="TotalOrders" Margin="0,0,0,5" FontSize="14"/>
                <TextBlock x:Name="PendingOrders" Margin="0,0,0,5" FontSize="14"/>
                <TextBlock x:Name="AvailableOrders" Margin="0,0,0,5" FontSize="14"/>
                <TextBlock x:Name="AssignedOrders" Margin="0,0,0,5" FontSize="14"/>
                <TextBlock x:Name="InProgressOrders" Margin="0,0,0,5" FontSize="14"/>
                <TextBlock x:Name="CompletedOrders" Margin="0,0,0,5" FontSize="14"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
