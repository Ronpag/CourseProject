<Window x:Class="CRM.View.RegisterUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register user"
        Height="300"
        Width="350"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Login"/>
        <TextBox x:Name="LoginBox" Grid.Row="1" Margin="0,5,0,10"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="3">
            <CheckBox x:Name="ActiveCheckBox" Content="Active" IsChecked="True"/>
        </StackPanel>

        <Button Grid.Row="4" Margin="0,15,0,0" Height="35" Content="Register" Click="RegisterBtn"/>

    </Grid>

</Window>